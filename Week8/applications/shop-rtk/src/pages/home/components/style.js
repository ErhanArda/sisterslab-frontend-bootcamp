import { Stack, styled } from '@mui/material';

const StyledCategoryListStack = styled(Stack)`
  border-radius: 4px;
  background: aliceBlue;
  padding: 20px;
  box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
  height: fit-content;
`;

export { StyledCategoryListStack };
